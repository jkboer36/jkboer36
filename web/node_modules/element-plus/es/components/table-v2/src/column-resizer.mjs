import '../../../utils/index.mjs';
import { column, styleType } from './common.mjs';
import { buildProps, definePropType } from '../../../utils/vue/props.mjs';

const tableV2ColumnResizerProps = buildProps({
  column: { ...column, required: true },
  onResize: {
    type: definePropType(Function)
  },
  onResizeStart: {
    type: definePropType(Function)
  },
  onResizeStop: {
    type: definePropType(Function)
  },
  minWidth: {
    type: Number,
    default: 30
  },
  style: styleType
});

export { tableV2ColumnResizerProps };
//# sourceMappingURL=column-resizer.mjs.map
